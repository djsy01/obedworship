# 📅 26년 2월 계획

> 26년 2월 계획 및 완료 현황입니다.

---

## 📌 월간 목표

- 라이트/다크 모드 대응
- 카드 호버 CSS 통일 및 다크모드 깨짐 수정
- 관리자 페이지 홈 음원 순서 관리 기능

---

## Front-End

| 주차  | 계획                                 | 상태    | 완료일 | 비고                                        |
| ----- | ------------------------------------ | ------- | ------ | ------------------------------------------- |
| 1주차 | 라이트/다크모드 추가                 | ✅ 완료 | 02/06  | light-dark() CSS 함수 적용                  |
| 1주차 | 카드 호버 inset box-shadow 통일      | ✅ 완료 | 02/06  | border-bottom → inset box-shadow 전환       |
| 1주차 | 다크모드 카드 하단 짤림 수정         | ✅ 완료 | 02/06  | 9개 카드 타입 일괄 수정                     |
| 1주차 | 관리자 페이지 음원 드래그앤드롭 구현 | ✅ 완료 | 02/06  | HTML5 Drag and Drop API (외부 라이브러리 X) |
| 1주차 | 홈 플레이어 음원 순서 동기화         | ✅ 완료 | 02/06  | MainLayout display_order 정렬 적용          |

---

## Back-End (담당자 진행)

| 주차 | 계획 | 상태 | 비고 |
| ---- | ---- | ---- | ---- |

---

## 📊 2월 완료 통계

- **Front-End**: 5개 작업 완료 (100%)
- **Back-End**: 담당자 별도 진행

---

## 🎯 2월 주요 성과

### Front-End

- ✨ 카드 호버 CSS 통일 (inset box-shadow 방식)
  - 기존 `border-bottom: 3px solid transparent` → `inset 0 -3px 0 var(--ow-accent)` 전환
  - 다크모드에서 하단 테두리 짤림 + 양쪽 끊김 문제 해결
  - 수정 대상 (7개 파일, 9개 카드):
    - Qna.css: `.qna-card`, `.stat-card`
    - Vision.css: `.highlight-box`, `.member-card`
    - WorshipLog.css: `.log-card`
    - Admin.css: `.admin-card`, `.stat-card`
    - MyPage.css: `.stat-box`, `.application-card`
    - Home.css: `.feature-card`
    - Tickets.css: `.ticket-card`
- ✨ 관리자 페이지 홈 음원 드래그앤드롭 순서 변경
  - HTML5 Drag and Drop API 사용 (외부 라이브러리 없음)
  - 드래그 핸들(⠿) + 시각적 피드백 (opacity, border highlight)
  - 드롭 시 `display_order` 순차 저장
- ✨ MainLayout 플레이어 음원 순서 동기화
  - `display_order` 기준 정렬 적용

---

## 💡 기술적 도전 과제

### 해결 완료 ✅

- 카드 호버 밑줄 양쪽 끊김: `border-bottom` + `border-radius` 조합에서 border 두께 차이로 모서리 깨짐 → `inset box-shadow`로 해결
- 다크모드 카드 하단 짤림: `border-bottom: transparent`가 다크모드에서 보이지 않아 하단 잘림 → `inset box-shadow`로 통일 해결
- NestJS `whitelist: true` DTO 필드 무시: class-validator 데코레이터 없는 필드가 `ValidationPipe`에서 제거됨 → `CreateAssetDto`에 데코레이터 추가

---

## 📝 다음 달(3월) 이월 예정

### 실배포 준비 (5개 페이지)

- **WorshipDetail** (`/worship-log/:id`)
  - [ ] 섹션 없을 때 빈 화면 대신 안내 문구 처리
  - [ ] 포스터 없을 때 fallback UI 개선
  - [ ] 날짜 포맷 영어 요일 → 한국어 변경
  - [ ] 모바일 레이아웃 검토

- **WorshipLog** (`/worship-log`)
  - [ ] 카드 디자인 품질 검토 (poster_url 썸네일 노출 여부 결정)
  - [ ] 빈 목록 상태 UI 개선

- **Home** (`/`)
  - [ ] Feature Cards 링크 정리 (Scores·QnA 비배포 → 카드 숨김 or 교체)
  - [ ] Hero 이미지 없을 때 fallback 처리

- **Vision** (`/vision`)
  - [ ] 멤버/사진 없을 때 빈 상태 UI 확인

- **Map** (`/map`)
  - [ ] 교통편 안내 텍스트 추가 여부 결정

### 공통

- [ ] MainLayout.vue 네비게이션 메뉴 항목 확인 (비배포 페이지 링크 숨김)
- [ ] 환경변수 (`VITE_API_BASE_URL` 등) 프로덕션 값 설정
- [ ] 빌드 오류 없음 확인 (`npm run build`)
- [ ] 불필요한 `console.log` 제거
- [ ] 성능 최적화 (이미지 lazy loading, code splitting)
- [ ] JWT 인증 연동
